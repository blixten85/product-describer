# Ollama
OLLAMA_MODEL=llama3.1:8b

# Sync mode (optional — only needed when running with --profile sync)
# DOCKER must point at the parent dir that contains scraper/credentials/api_key
SCRAPER_URL=https://scraper.example.com
SYNC_INTERVAL=300
