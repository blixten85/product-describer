services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - ./data:/app/uploads
      - ./data:/app/outputs
    restart: unless-stopped
